import React from "react";
import * as FontAwesome from "react-icons/fa";

class FaIcon extends React.Component<{ icon: string, size: number, color: string }> {

    render() {
        // const { icon, size, color } = this.props;
        // const el = React.createElement(FontAwesome[icon]);
        // return <div style={{ fontSize: size, color: color }}>{el}</div>;
        return <div></div>
    }
}

export default FaIcon;